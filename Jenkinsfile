pipeline {
    agent any

    stages {
        stage('AI DevOps Support') {
            steps {
                sh '''
                  curl -s "http://localhost:8000/chat?query=jenkins build failed"
                '''
            }
        }
    }
}
